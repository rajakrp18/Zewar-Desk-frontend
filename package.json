{
  "name": "zewar-desk-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Zewar Desk ERP - Jewellery Business Management System",
  "scripts": {
    "dev": "cd zewar-desk-erp && npm run dev",
    "build": "cd zewar-desk-erp && npm run build",
    "start": "cd zewar-desk-erp && npm start",
    "lint": "cd zewar-desk-erp && npm run lint",
    "type-check": "cd zewar-desk-erp && npm run type-check",
    "install-deps": "cd zewar-desk-erp && npm install"
  },
  "dependencies": {},
  "devDependencies": {}
}
